ContentsView.swift and SampleTranslatorApp.swift are
the projects I built... But you probably built your
own. If so, ignore those files.

The important files are ParserLibrary.swift and
SampleTranslator.swift. You will be making small
changes to the sample translator.